mixin Select-Category(items, level=1)
  each item in items
    - const prefix=Array(level + 1).join('--&nbsp;&nbsp;')
    option(value=item._id)!= prefix + item.category_name 
      if item.children && item.children.length>0
        +Select-Category(item.children, level+1)