head 
  link(rel="stylesheet", href="/client/home/assets/css/register.css")
  include ../../layouts/main.pug
body
  .form-container
    .form-header
      h1 Đăng ký tài khoản
      p Vui lòng điền thông tin để tạo tài khoản mới
    form#registerForm(action=`/user/register` method="POST" novalidate)
      .form-group
        label(for="name") Họ và tên
        .input-group
          input#fullname.form-control(type="text", name="name" placeholder="Nhập họ và tên của bạn", required)
      .form-group
        label(for="email") Email
        .input-group
          input#email.form-control(type="email",name="email" placeholder="Nhập địa chỉ email", required)
      .form-group
        label(for="phone") Số điện thoại
        .input-group
          input#phone.form-control(type="tel",name="phone" placeholder="Nhập số điện thoại", pattern="[0-9]{10}", required)
      .form-group
        label(for="password") Mật khẩu
        .input-group
          input#password.form-control(type="password",name="password" placeholder="Nhập mật khẩu", required)
          button.password-toggle(type="button")
            i.fas.fa-eye#toggleIcon
      button.submit-btn(type="submit") Đăng ký
  script(type="module", src="/client/home/assets/js/Register.js") 